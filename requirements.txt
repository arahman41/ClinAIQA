# Pinned for reproducibility. Update deliberately and record reason in commit message.

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1

# Data validation and settings
pydantic==2.10.3
pydantic-settings==2.7.0

# Database (async driver for app, sync driver for Alembic)
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10
pgvector==0.3.6

# LLM client
anthropic==0.40.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# Explainability (Phase 3+)
shap==0.46.0
numpy==2.2.0
